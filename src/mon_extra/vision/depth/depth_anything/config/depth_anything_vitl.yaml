# Basic
hostname    : ~
config      : ~
arch        : "depth_anything"
model       : "depth_anything_vitl"
data        : ~
data_dir    : ~
root        : ~
project     : ~
variant     : ~
fullname    : "depth_anything_vitl"
save_dir    : ~
weights     : "zoo/vision/depth/depth_anything/depth_anything_vitl/pretrained/depth_anything_vitl.pth"
device      : 0
local_rank  : -1
launcher    : ~
epochs      : 200
steps       : ~
imgsz       : 518
resize      : ~
benchmark   : True
save_image  : True
save_debug  : False
use_data_dir: False
use_fullpath: False
exist_ok    : False
verbose     : True

# Model Specific
encoder     : "vitl"
features    : 256
out_channels: [256, 512, 1024, 1024]
pred_only   : True
format      : 2  # 0: grayscale, 1: color, 2: both
