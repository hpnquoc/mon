# Basic
hostname    : ~
config      : ~
arch        : "depth_anything"
model       : "depth_anything_vitb"
data        : ~
data_dir    : ~
root        : ~
project     : ~
variant     : ~
fullname    : "depth_anything_vitb"
save_dir    : ~
weights     : "zoo/vision/depth/depth_anything/depth_anything_vitb/pretrained/depth_anything_vitb.pth"
device      : 0
local_rank  : -1
launcher    : ~
epochs      : 200
steps       : ~
imgsz       : 518
resize      : ~
benchmark   : True
save_image  : True
save_debug  : False
use_data_dir: False
use_fullpath: False
exist_ok    : False
verbose     : True

# Model Specific
encoder     : "vitb"
features    : 128
out_channels: [96, 192, 384, 768]
pred_only   : True
format      : 2  # 0: grayscale, 1: color, 2: both
